package manual.pubsub.globalcomponents;

import plugins.globals.support.GlobalComponentId;

public enum GlobalComponent implements GlobalComponentId {
	ALPHA, BETA, GAMMA, DELTA, POPULATION_LOADER;
}
